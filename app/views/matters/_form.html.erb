<%= form_with(model: matter, url: [@company, matter], local: true) do |form| %>
  <% if matter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(matter.errors.count, "error") %> prohibited this matter from being saved:</h2>

      <ul>
      <% matter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :案件名 %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :案件詳細 %>
    <%= form.text_field :detail %>
  </div>

  <div class="field">
    <%= form.label :担当者 %>
    <%= form.text_field :contact_person %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
