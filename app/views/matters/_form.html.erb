<%= form_with(model: matter, url: [@company, matter], local: true) do |form| %>
  <% if matter.errors.any? %>
    <div id="error_explanation" class="alert-danger">
      <h2><%= pluralize(matter.errors.count, "error") %> 登録に失敗しました:</h2>

      <ul>
      <% matter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :detail %>
    <%= form.text_field :detail %>
  </div>

  <div class="field">
    <%= form.label :contact_person %>
    <%= form.text_field :contact_person %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
